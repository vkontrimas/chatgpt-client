import './Auth.css'

const Input = ({ isPassword }) => {
  return (
    <input />
  )
}

const Auth = () => {
  return (
    <form className="auth-form">
      <Input />
      <Input />
    </form>
  )
}

export default Auth
