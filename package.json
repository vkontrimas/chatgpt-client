{
  "scripts": {
    "test": "NODE_ENV='test' cd back && npm test && cd ../web && npm test",
    "build-frontend": "cd web && npm run build -- --outDir '../back/public'",
    "heroku-prebuild": "./scripts/build_prod.sh",
    "start": "cd back && npm start"
  }
}
